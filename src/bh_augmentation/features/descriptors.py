"""Molecular descriptor helpers.

The first MVP keeps descriptor support minimal. Morgan fingerprints are exposed
from `bh_augmentation.features.featurize`; richer descriptor sets can be added
later without changing the data-loading or modeling code.
"""
